<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="cn.edu.pku.wu.choosedormitory">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"  />
            </intent-filter>
        </activity>
        <activity android:name=".message"></activity>
        <activity android:name=".louhao"></activity>
        <activity android:name=".kongchuangwei"></activity>
        <activity android:name=".studentnumber"></activity>
        <activity android:name=".sanren"></activity>
        <activity android:name=".siren"></activity>
        <activity android:name=".shuangren"></activity>
        <activity android:name=".success"></activity>


    </application>

</manifest>
